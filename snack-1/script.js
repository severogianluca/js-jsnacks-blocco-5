const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

const  result = names.forEach((element) =>{ 
    console.log(element)
})




for (let i = 0; i < names.length; i++) {
    let list;
    list = names[i];
    console.log(list);

}

